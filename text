import os
import json
import boto3
import base64
import secrets
import string
import subprocess
import time

# ================= ENV =================
REGION = os.environ["AWS_REGION"]

DB_CLUSTER_ID = os.environ["DB_INSTANCE_ID"]
SSM_PARAMETER_NAME = os.environ["SSM_PARAMETER_NAME"]
EKS_CLUSTER_NAME = os.environ["EKS_CLUSTER_NAME"]

HELM_RELEASE = os.environ["HELM_RELEASE"]
HELM_CHART = os.environ["HELM_CHART"]
HELM_NAMESPACE = os.environ["HELM_NAMESPACE"]

DEPLOYMENT_NAME = os.environ["DEPLOYMENT_NAME"]
REPLICA_COUNT = os.environ["REPLICA_COUNT"]

# ================= BIN PATHS =================
HELM = "/opt/bin/helm"
KUBECTL = "/opt/bin/kubectl"

# ================= AWS CLIENTS =================
rds = boto3.client("rds", region_name=REGION)
ssm = boto3.client("ssm", region_name=REGION)
eks = boto3.client("eks", region_name=REGION)

# ================= HELPERS =================

def generate_password(length=32):
    """Aurora-safe password"""
    chars = string.ascii_letters + string.digits + "!#$^*_-+=?"
    return ''.join(secrets.choice(chars) for _ in range(length))


def write_kubeconfig():
    cluster = eks.describe_cluster(name=EKS_CLUSTER_NAME)["cluster"]

    endpoint = cluster["endpoint"]
    ca_data = cluster["certificateAuthority"]["data"]

    sts = boto3.client("sts", region_name=REGION)
    presigned_url = sts.generate_presigned_url(
        "get_caller_identity",
        Params={},
        ExpiresIn=60,
        HttpMethod="GET"
    )

    token = "k8s-aws-v1." + base64.urlsafe_b64encode(
        presigned_url.encode("utf-8")
    ).decode("utf-8").rstrip("=")

    kubeconfig = f"""
apiVersion: v1
kind: Config
clusters:
- name: eks
  cluster:
    server: {endpoint}
    certificate-authority-data: {ca_data}
users:
- name: eks-user
  user:
    token: {token}
contexts:
- name: eks
  context:
    cluster: eks
    user: eks-user
current-context: eks
"""

    with open("/tmp/kubeconfig", "w") as f:
        f.write(kubeconfig)

    os.environ["KUBECONFIG"] = "/tmp/kubeconfig"


def apply_db_secret(password):
    """Create or update Kubernetes Secret safely"""
    p = subprocess.Popen(
        [
            KUBECTL, "create", "secret", "generic", "nexus-db-secret",
            "-n", HELM_NAMESPACE,
            f"--from-literal=password={password}",
            "--dry-run=client", "-o", "yaml"
        ],
        stdout=subprocess.PIPE
    )

    subprocess.check_call(
        [KUBECTL, "apply", "-f", "-"],
        stdin=p.stdout
    )


def run(cmd):
    subprocess.check_call(cmd)


# ================= LAMBDA HANDLER =================

def lambda_handler(event, context):

    # Generate new password
    new_password = generate_password()

    # Rotate Aurora DB password
    rds.modify_db_cluster(
        DBClusterIdentifier=DB_CLUSTER_ID,
        MasterUserPassword=new_password,
        ApplyImmediately=True
    )

    # Give Aurora time to apply password
    time.sleep(30)

    # Update SSM Parameter
    ssm.put_parameter(
        Name=SSM_PARAMETER_NAME,
        Value=new_password,
        Type="SecureString",
        Overwrite=True
    )

    # Prepare kubeconfig
    write_kubeconfig()

    # Create / update Kubernetes Secret
    apply_db_secret(new_password)

    # Helm upgrade (NO PASSWORD INLINE)
    run([
        HELM, "upgrade", HELM_RELEASE, HELM_CHART,
        "-n", HELM_NAMESPACE,
        "--reuse-values",
        "--set-string", "database.existingSecret=nexus-db-secret"
    ])

    # Scale DOWN Nexus IQ
    run([
        KUBECTL, "scale", "deployment", DEPLOYMENT_NAME,
        "-n", HELM_NAMESPACE,
        "--replicas=0"
    ])

    #  Scale UP Nexus IQ
    run([
        KUBECTL, "scale", "deployment", DEPLOYMENT_NAME,
        "-n", HELM_NAMESPACE,
        f"--replicas={REPLICA_COUNT}"
    ])

    return {
        "status": "SUCCESS",
        "message": "Aurora password rotated, SSM updated, Helm upgraded, Nexus IQ restarted"
    }
