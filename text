    Wait Until Element Is Visible    xpath=//div[contains(@class,'gl-search-box-results')]    timeout=10s

    # CLICK the lower "slug" line → the true project link
    Click Element    xpath=//div[contains(@class,'gl-search-box-results')]//a[.//span[contains(., '${PROJECT_NAME}')]]

