import os
import boto3
import base64
import secrets
import string
import subprocess
import json
import tempfile
from botocore.signers import RequestSigner

# ================= ENV =================
REGION = os.environ["AWS_REGION"]

DB_CLUSTER_ID = os.environ["DB_INSTANCE_ID"]
SSM_PARAMETER_NAME = os.environ["SSM_PARAMETER_NAME"]
EKS_CLUSTER_NAME = os.environ["EKS_CLUSTER_NAME"]

HELM_RELEASE = os.environ["HELM_RELEASE"]
HELM_CHART = os.environ["HELM_CHART"]
HELM_NAMESPACE = os.environ["HELM_NAMESPACE"]

DEPLOYMENT_NAME = os.environ["DEPLOYMENT_NAME"]
REPLICA_COUNT = os.environ["REPLICA_COUNT"]

# Ensure kubectl / helm from Lambda Layer
os.environ["PATH"] += ":/opt/bin"

# ================= AWS CLIENTS =================
rds = boto3.client("rds", region_name=REGION)
ssm = boto3.client("ssm", region_name=REGION)
eks = boto3.client("eks", region_name=REGION)
sts = boto3.client("sts", region_name=REGION)

# ================= HELPERS =================

def generate_password(length=32):
    chars = string.ascii_letters + string.digits + "!#$^*_-+=?"
    return ''.join(secrets.choice(chars) for _ in range(length))


def get_eks_token(cluster_name):
    session = boto3.session.Session(region_name=REGION)
    credentials = session.get_credentials()

    sts_client = session.client("sts")
    service_id = sts_client.meta.service_model.service_id

    signer = RequestSigner(
        service_id=service_id,
        region_name=REGION,
        signing_name="sts",
        signature_version="v4",
        credentials=credentials,
        event_emitter=session.events,
    )

    request_params = {
        "method": "GET",
        "url": f"https://sts.{REGION}.amazonaws.com/"
               "?Action=GetCallerIdentity&Version=2011-06-15",
        "body": {},
        "headers": {
            "x-k8s-aws-id": cluster_name
        },
        "context": {},
    }

    presigned_url = signer.generate_presigned_url(
        request_dict=request_params,
        operation_name="GetCallerIdentity",
        expires_in=60,
    )

    token = "k8s-aws-v1." + base64.urlsafe_b64encode(
        presigned_url.encode("utf-8")
    ).decode("utf-8").rstrip("=")

    return token


def write_kubeconfig():
    cluster = eks.describe_cluster(name=EKS_CLUSTER_NAME)["cluster"]

    token = get_eks_token(EKS_CLUSTER_NAME)

    kubeconfig = {
        "apiVersion": "v1",
        "kind": "Config",
        "clusters": [{
            "name": "eks",
            "cluster": {
                "server": cluster["endpoint"],
                "certificate-authority-data": cluster["certificateAuthority"]["data"]
            }
        }],
        "contexts": [{
            "name": "eks",
            "context": {
                "cluster": "eks",
                "user": "lambda"
            }
        }],
        "current-context": "eks",
        "users": [{
            "name": "lambda",
            "user": {
                "token": token
            }
        }]
    }

    with open("/tmp/kubeconfig", "w") as f:
        json.dump(kubeconfig, f)

    os.environ["KUBECONFIG"] = "/tmp/kubeconfig"


def run(cmd):
    subprocess.check_call(cmd)


# ================= LAMBDA HANDLER =================

def lambda_handler(event, context):

    
    print("CALLER IDENTITY:", sts.get_caller_identity())

    # Generate new DB password
    new_password = generate_password()

    # Rotate Aurora password
    rds.modify_db_cluster(
        DBClusterIdentifier=DB_CLUSTER_ID,
        MasterUserPassword=new_password,
        ApplyImmediately=True
    )

    # Update SSM
    ssm.put_parameter(
        Name=SSM_PARAMETER_NAME,
        Value=new_password,
        Type="SecureString",
        Overwrite=True
    )

    # Prepare kubeconfig (EKS auth)
    write_kubeconfig()

    # Helm upgrade
    run([
        "helm", "upgrade", HELM_RELEASE, HELM_CHART,
        "-n", HELM_NAMESPACE,
        "--reuse-values",
        "--set-string", f"database.password={new_password}"
    ])

    # Scale DOWN deployment
    run([
        "kubectl", "scale", "deployment", DEPLOYMENT_NAME,
        "-n", HELM_NAMESPACE,
        "--replicas=0"
    ])

    # Scale UP deployment
    run([
        "kubectl", "scale", "deployment", DEPLOYMENT_NAME,
        "-n", HELM_NAMESPACE,
        f"--replicas={REPLICA_COUNT}"
    ])

    return {
        "status": "SUCCESS",
        "message": "Aurora password rotated, SSM updated, Helm upgraded, deployment restarted"
    }
